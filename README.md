# js2020
```bash

mkdir ndsql

cd ndsql

/ndsql>npm init --yes 
Wrote to \ndsql\package.json:
{ "name": "ndsql", "version": "1.0.0", "description": "", "main": "index.js", "scripts": { "test": "echo "Error: no test specified" && exit 1" }, "keywords": [], "author": "", "license": "ISC" }

ndsql>npm install express mysql --save (or  --save --save-exact)

-- ndsql\views>notepad list.ejs

ndsql>npm install ejs --save
ndsql>nodemon app.js

```
